# 去中心化改造与实测 · 2026-09-21

> 文中 `outputs/...` 目录与 `127.0.0.1:8091/?run=...` 链接指向作者本地运行记录，不随仓库发布；精选运行以预置回放形式附带在 `src/jev_mapf/presets/`。

## 2026-09-22：16 张官方 puzzle，v4 → v5 无线电 → 协作规划

统一设置：`puzzle-00..15` 原图，4 智能体，seed 42，观测半径 3，32 步上限，soft 碰撞，不消失目标。联合状态穷举 BFS 显示 15/16 可解（`puzzle-06` 无解），最优 makespan 4–20；`puzzle-04` 与 `puzzle-08` 生成的起终点完全相同。

| 方法 | 解出 | 平均 ISR | 说明 |
| --- | ---: | ---: | --- |
| 独立 A*（`p16-astar-*`） | 4/16 | 0.69 | 互锁 |
| 全图 PIBT（离线脚本） | 5/16 | — | 反应式上限参考，非本仓库策略 |
| DeepSeek v4 不思考（`p16-ds-*`） | 3/16 | 0.66 | 双方同时让行、同时返回 |
| DeepSeek v5 无线电（`p16v5-ds-*`） | 3/16 | 0.53 | 让行规则遵守约一半 |
| Jev v5 无线电（`p16v5-jev-*`，前 7 张） | 2/7 | — | 让行遵守 18/18；3 张网络错误 |
| **DeepSeek + 协作规划**（`p16coop-ds-*`） | **15/16** | 0.97 | 全部为最优步数；采纳建议 516/516 |
| **Jev + 协作规划**（`p16coop-jev-*`） | **15/16** | 0.94 | 全部为最优步数；`puzzle-00/01/06/07` 曾因 ConnectError 中断，重跑后完成 |
| Qwen 1.5B + 协作规划（`p16coop-qwen-*`） | 0/16 | 0.12 | 采纳 863/2048，与随机相近 |

- v5 无线电：传感范围内交换优先级与下一步想去格子，含优先级继承；对 DeepSeek 没有提高解出数，因为 puzzle 需要多步前瞻，反应式规则上限约等于 PIBT。
- 首版协作规划中，v5 提示“通行权优先于一切”与计划冲突，DeepSeek 在 `puzzle-00` 反复让行而失败（记录移至 `outputs/superseded/`）；改为“有计划建议时覆盖通行权规则”后重跑为上表。
- 协作规划组内共享观测地图与目标，结果只能记为「去中心化 + 协作规划」。联合 A* 单拍最长约 0.8 秒；无线电范围取 2× 观测半径，以免已到目标的机器人离组后被组内计划穿过。
- 用量：DeepSeek 约 152 万 tokens、单次调用中位 728 ms；Jev 约 151 万 tokens、中位 590 ms（不含重试）；Qwen 本地中位 1.6 s。
- seed 42 单次运行；Jev 网络错误记录以 `neterr-` 前缀保留。

## 2026-09-22：v4 自身地图距离（尚无真实模型结果）

诊断：v3 之前的 21×21 迷宫运行 `6cb7d625b022` 中 384 个动作有 174 次等待、92 次右移与 93 次左移，ISR=0.5；模型只有直线 `goal_delta`，无法从 7×7 视野推断绕墙路线。v4 为每个候选增加基于**自己**观测地图的 BFS 距离（未知格可通行，忽略机器人），仍不共享他人信息、不做联合规划或动作替换。

信号检验（**不是模型成绩**）：一个只读取智能体实际输入的脚本化贪心规则（避开可见占用格、取最小距离、连续停滞 2 步随机让行），在 20 张 `validation-mazes-seed-000..019` 与 `puzzle-03`、`four_rooms`、`passing_bay`、`crossing` 共 24 例、seed 42、128 步上限下：用直线曼哈顿距离时 CSR=0.08、ISR=0.51；用 v4 距离时 CSR=0.62、ISR=0.85。剩余失败主要来自多智能体互锁，需由模型的让行判断解决。Jev / DeepSeek 在 v4 下的真实运行见下表。

### v4 真实运行：官方 `puzzle-03`，4 智能体，seed 42，观测半径 3

起终点与 v3 记录相同。每个智能体独立请求，无 A* 回退。“跟随 −1”指未在目标、且存在空的距离 −1 候选时，模型选中它的比例。

| 记录（`outputs/` 下） | 策略 / 配置 | 步数 | 请求 / HTTP 200 | 结果 | SoC | 跟随 −1 |
| --- | --- | ---: | ---: | --- | ---: | ---: |
| v4-astar-puzzle-03 | POGEMA 独立 A*（96 步上限） | 96 | 0 / 0 | ISR=0.75，互锁 | 140 | — |
| v4-jev-puzzle03-r8 | Jev `jev-1.13.0`，`JEV_MAX_ATTEMPTS=8` | 13 | 64 / 52 | **CSR=1** | 46 | 30/32 |
| v4-dsfast-puzzle03 | `deepseek-flash` 不思考，1024 token | 14 | 56 / 56 | **CSR=1** | 44 | 29/30 |
| v4-ds16k-puzzle03 | `deepseek-flash` low 思考，16384 token | 12 | 48 / 48 | **CSR=1** | 39 | 25/28 |
| v4-ds-puzzle03 | `deepseek-flash` low 思考，8192 token | 4 | 20 / 20 | 输出预算耗尽，停止 | — | — |
| v4-jev-puzzle03 | Jev，默认 3 次尝试 | 0 | 11 / 1 | ConnectError，未执行 | — | — |
| v4-qwen-puzzle03 | 本地 Qwen2.5-1.5B RLCD | 32 | 128 / 128 | ISR=0 | 128 | 49/127 |
| v4-qwen-puzzle03-labels | Qwen，标签内附距离与占用 | 32 | 128 / 128 | ISR=0，全员两格振荡 | 128 | 64/127 |

- Jev 失败原因为本机到 `api.typesafe.ai` 的 TLS 握手间歇失败（curl 3 次中 2 次 error 35），不是决策失败；提高重试上限后 12 次重试被记录，最终成功。
- DeepSeek 思考模式 8192 token 不足；不思考最便宜（105k tokens），16384 预算步数最少但用时约 8.6 分钟、198k tokens。
- Qwen 1.5B 去中心化首次实连：协议与原生概率正常，但几乎不利用距离信号，从第 1 步起陷入确定性的两格振荡；在标签中加入“反转上一步/重复次数”后轨迹完全相同。判断为模型容量限制，不建议作为演示主策略。
- seed 42，单张地图。


## 2026-09-22：四智能体观测与指引修复

保持官方 `puzzle-03` 原图、4 个智能体、观测半径 3、32 步上限、Jev 原生 Choice、不消失目标、去中心化独立决策。seed 42 的起点为 `[[3,4],[3,2],[2,4],[1,2]]`，目标为 `[[2,1],[1,4],[2,2],[4,4]]`。

| 记录（`outputs/` 下） | 版本 / seed | 步数 | 请求 / HTTP 200 | 最终到达 | SoC | 说明 |
| --- | --- | ---: | ---: | --- | ---: | --- |
| 9fb3bedebd0f | v1 / 42 | 32 | 128 / 128 | 0/4 | 128 | 22 轮全体选择 wait |
| jev-puzzle03-four-v2-seed42 | v2 / 42 | 32 | 128 / 128 | 1/4 | 127 | 打破等待，但发生两格振荡及到目标后离开 |
| jev-puzzle03-four-v3-seed42 | v3 / 42 | 14 | 56 / 56 | 4/4 | 40 | CLI 真实成功；1 次动作阻挡 |
| 0c67522a5477 | v3 / 42 | 14 | 56 / 56 | 4/4 | 40 | 网页后端独立重跑成功，已部署 v3 |
| jev-puzzle03-four-v3-seed43 | v3 / 43 | 28 | 112 / 112 | 4/4 | 34 | 额外 seed 检查成功，仍有较多绕行 |

v3 返回模型均为 `jev-1.13.0`：两个 seed、三次运行成功。提示词在 seed 42 上调试，seed 43 为一次额外检查。

修改针对真实失败机制：

- 将分离的二值矩阵改为带 S/G/A/X 标记的局部视图，直接列出可见邻居及其当前可见空出口；仍保留个人静态观测记忆，不输入他人目标或计划。
- 候选仍是最多 5 个原始一步动作，仅过滤墙体。增加目的格占用、动作后的目标相对位移、相邻格状态、近期在此受阻次数、即时折返和同一位移重复次数。这些是自己的观测/历史事实，不是路径搜索、动作评分或推荐答案。
- 分别统计主动 wait 与移动被阻挡造成的停滞；未到目标且有空位时，提示优先让行或绕行。
- v2 中反停滞提示误伤已到目标的机器人，v3 明确“目标上的等待是完成而非停滞”，并将两格振荡单独标记，要求继续绕行而不是立即折返。

36 项自动化测试通过，覆盖原有隔离约束、语义坐标、传感器外动态占用未知、邻居出口、停滞/等待/阻挡区别、目标等待、振荡标记，以及保留所有非撞墙候选。网页核验了四智能体成功、v3 版本显示、逐人真实输入及重跑按钮；Key 仍仅在服务内存，不在记录中。

成功回放：`http://127.0.0.1:8091/?run=0c67522a5477`。每次原始请求含完整 instructions，配置与状态记录 prompt_version，可与旧版本直接对照。

## 2026-09-21：官方 5×5 puzzle 小地图

用户要求降低地图难度后，选用原始 `puzzle-03` 布局，seed 42、局部观测半径 3、32 步上限，仍使用 Jev 独立请求、个人记忆和 POGEMA 同步执行。未新增规划器、全局动作协调器或成功回退。

| 输出目录（`outputs/` 下） | 智能体数 | 步数 | 请求 / HTTP 200 | 结果 |
| --- | ---: | ---: | ---: | --- |
| 1ad2067c2491 | 4 | 32 | 128 / 128 | 达到上限，ISR=0，11 次动作阻挡，后期持续等待 |
| 1a3ebd903554 | 2 | 8 | 16 / 16 | CSR=1，ISR=1，SoC=12，makespan=8，零阻挡 |

两智能体成功运行的真实返回模型为 `jev-1.13.0`，合计 23,551 tokens；起点 `[[3,4],[3,2]]`，目标与最终位置均为 `[[2,1],[1,4]]`。逐条核验 16 个实际请求均为私有局部状态、每人最多 5 个候选，执行选择均来自 Jev，没有 A* 回退。局部 A* 对照在同一两智能体案例中用 11 步完成，但不是上述 Jev 轨迹来源。

两智能体、固定 seed 的成功案例；四智能体失败记录保留。回放：`http://127.0.0.1:8091/?run=1a3ebd903554`。

切换前当前大迷宫运行 `6cb7d625b022` 已达到 96 步上限，384 次请求全部 HTTP 200，ISR=0.5；因此这次用户看到的较差表现并非之前的 TLS 中断。

## 首次去中心化大迷宫测试

本节为当前默认模式；后面的旧集中式成功不计入去中心化结果。

地图：官方 `validation-mazes-seed-000` 原始 21×21 布局，源 commit `1209772eedc085fd2ce10dd459b7df60760aade7`，seed 42，4 个智能体、局部半径 3、96 步上限、经典不消失 MAPF。不是对官方完整 benchmark 协议的复现。

每个智能体只接收自己的 7×7 观测、私有坐标/目标、私有观测记忆和自己的最近 8 步反馈。最多 5 个动作，仅过滤局部墙体，没有中央联合动作筛选或动作替换。各人请求独立；同一进程只汇总动作并同步执行。全局帧仅用于评估/展示。

| 输出目录（`outputs/` 下） | 执行步数 | 请求尝试 / HTTP 200 | 结果 |
| --- | ---: | ---: | --- |
| astar-local-maze000-seed42 | 23 | 0 / 0 | CSR=1，ISR=1，SoC=69，makespan=23，3 次阻挡 |
| jev-local-maze000-seed42 | 6 | 28 / 25 | 第 7 轮连接失败，整轮未执行；CSR=0，ISR=0 |
| jev-local-maze000-seed42-v2 | 1 | 17 / 7 | 加入两次有界重试，仍出现 ConnectError；CSR=0，ISR=0 |
| jev-local-maze000-seed42-proxy | 0 | 7 / 3 | 使用已有本机代理，1 个智能体重试耗尽，整轮未执行 |

上述 Jev 真实响应均返回 `jev-1.13.0`，包含原生 choice 和完整候选概率。三个实验合计 52 次请求尝试、35 个 HTTP 200 响应、46,422 tokens，**没有一个完成该地图**。无凭据网络诊断复现了 TLS `UNEXPECTED_EOF_WHILE_READING`，故障点在连接层。失败过程全部保留。

30 项自动化测试通过，包括真实出站 payload 隔离、其他智能体目标变化不影响当前输入、局部静态墙过滤、相互冲突动作原样交给环境、单人失败不推进、重试次数/证据和旧模式回归。浏览器已验收 145 张官方地图入口、局部 A* 23 步完成、单智能体输入检查、时间轴、导出和移动布局。

```bash
.venv/bin/jev-mapf run --case validation-mazes-seed-000 --provider jev \
  --num-agents 4 --obs-radius 3 --max-steps 96 --key-stdin \
  --output outputs/jev-local-next
```

## 历史：集中式首次闭环验证

所有模型实验使用此前保存的 DS 连接，API 返回模型均为 `deepseek-flash`。地图始终为同一个 `crossing` 案例：6×6、2 个智能体、seed 42、经典不消失模式、soft 碰撞系统，模型实验上限 24 步。

| 输出目录（`outputs/` 下） | 输入 / 思考配置 | 真实调用数 | 已执行步数 | 结果 |
| --- | --- | ---: | ---: | --- |
| astar-crossing | POGEMA A* 基线 | 0 | 10 | CSR=1，SoC=20 |
| deepseek-crossing | 数字候选；默认思考；512 token | 1 | 0 | 最终 JSON 为空，停止 |
| deepseek-crossing-v2 | 数字候选；不思考；512 token | 24 | 24 | 达到步数上限，ISR=0 |
| deepseek-crossing-v3 | 动作名+一步坐标；low 思考；4096 token | 6 | 5 | 第 6 次输出预算耗尽，停止 |
| deepseek-crossing-descriptive-fast | 动作名+一步坐标；不思考；512 token | 24 | 24 | 达到步数上限，ISR=0 |
| deepseek-crossing-v4 | 动作名+一步坐标；low 思考；8192 token | 10 | 10 | **CSR=1，ISR=1，SoC=20，makespan=10** |

成功模型实验累计 API 等待 106.67 秒、38,093 tokens、10 个不同 response ID；所有响应均 HTTP 200。逐步核对返回 choice 属于当轮菜单、候选坐标等于环境执行后坐标、没有顶点或边交换冲突、最终位置等于各自目标。10 步等于这两个起终点的曼哈顿距离下界。

这是一轮开发与适配记录。思考模式与动作映射都影响了结果，无法归因到单一因素；非思考对照仍然失败。

成功案例回放：启动工作台后访问 `http://127.0.0.1:8091/?run=deepseek-crossing-v4`，或在「已保存实验」中选择对应运行。

```bash
.venv/bin/jev-mapf run --case crossing --provider deepseek \
  --mode centralized --obs-radius 5 --use-embodied-deepseek --max-steps 24 --max-tokens 8192 \
  --output outputs/deepseek-new-run
```

模型服务即便相同配置也可能返回不同轨迹。基线已验证重复一致；真实模型重跑应记录新目录。

其他三个任务做了 A* 环境冒烟验证：`passing_bay` 54 步、`four_rooms` 48 步、`random(seed=42)` 11 步成功。这些不是 DeepSeek 成绩。旧集中式阶段 Qwen 和 TypeSafe Jev 仅通过模拟传输协议测试；当前 Jev 实连情况以上方去中心化记录为准。

本机依赖版本见 `requirements-tested.txt`。浏览器已验证桌面与手机布局、模型接口字段、新建、单步、连续运行、指标、时间轴、JSON 导出。代码契约测试涵盖候选枚举与 POGEMA 一致性、失败不回退、输出截断、在途暂停、重置互斥、凭据不回显、原始动作不可被仿真改写和历史回放。
